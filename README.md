# Проектная работа: Вёрстка проекта

<img width="1600" height="1024" alt="page_02" src="https://github.com/user-attachments/assets/409e7c10-404a-451c-abca-1262db909b4d" />

## О проекте

Интерактивное приложение на React и TypeScript для настройки оформления статьи: через выезжающую панель можно менять типографику, ширину колонки, отступы и тему, а выбранные параметры применяются к странице через CSS-переменные.

Проект выполнен в рамках курса "Фронтенд-разработчик" в Яндекс Практикуме. В качестве основы использован шаблон [`yandex-praktikum/blog-customizer`](https://github.com/yandex-praktikum/blog-customizer), поверх которого реализована прикладная логика и композиция интерфейса.

## 🔗 Ссылки

- Репозиторий: https://github.com/morfekta/blog-customizer  
- [Макет в Figma](https://www.figma.com/file/FEeiiGLOsE7ktXbPpBxYoD/Custom-dropdown?type=design&node-id=0%3A1&mode=design&t=eXRJnWC6Xsuw0qR4-1)

## Функциональность

**Сайдбар с настройками**

- Открытие / закрытие панели по клику на стрелку.  
- Закрытие при повторном клике или клике вне панели.  
- Фокус и управление с клавиатуры (Tab-навигация по контролам формы).

**Форма кастомизации**

Настройки сгруппированы по смыслу:

- **Типографика**:
  - шрифт;
  - базовый размер шрифта текста;
  - цвет шрифта.
- **Ширина и цвет**:
  - цвет фона страницы;
  - ширина текстовой колонки.

**Применение и сброс настроек**

- При изменении значений в сайдбаре **стили не применяются сразу** — состояние формы живёт отдельно.  
- Кнопка **«Применить»**:
  - копирует значения из формы в состояние страницы;
  - обновляет соответствующие CSS-переменные;
  - визуально обновляет статью.
- Кнопка **«Сбросить»**:
  - возвращает форму к начальному состоянию (значения, которые были при загрузке страницы);
  - применяет те же значения к статье;
  - CSS-переменные возвращаются к дефолтам.

**CSS-переменные**

- Все пользовательские настройки пробрасываются в оформление через CSS custom properties (`--font-size` и т.д.).  
- Начальные значения заданы в стилях, а приложение лишь меняет их на лету, не ломая архитектуру стилей.

## Мой вклад

В шаблоне Практикума [`yandex-praktikum/blog-customizer`](https://github.com/yandex-praktikum/blog-customizer) уже были настроены окружение (Webpack, TypeScript, Storybook, линтеры) и набор атомарных/молекулярных компонентов.

В рамках проектной работы я:

- Собрала из готовых компонентов финальный интерфейс страницы: шапка, статья, выезжающая панель настроек.
- Реализовала механику сайдбара:
  - управление открытием/закрытием;
  - закрытие по клику вне панели;
  - передача актуальных настроек в форму при открытии.
- Спроектировала и реализовала состояние:
  - отдельное состояние «страницы» (применённые настройки);
  - отдельное состояние «формы» (черновые изменения);
  - логику синхронизации при «Применить» и «Сбросить».
- Настроила маппинг полей формы на CSS-переменные.
- Добавила обработку крайних случаев:
  - сброс к дефолтам после серии изменений;
  - корректная работа при повторных применениях.
- Поддержала инфраструктуру проекта:
  - запуск и использование Storybook для изоляции компонентов;
  - соблюдение настроек ESLint и Stylelint;
  - форматирование кода (Prettier) и стилистическое выравнивание под требования курса.

## Технологии

- **Язык и платформа**:  
  - React  
  - TypeScript  
- **Стили**:  
  - SCSS / CSS-модули;  
  - CSS custom properties для пользовательских настроек.
- **Инструменты разработки**:
  - Storybook для разработки компонентов;
  - Webpack в качестве сборщика;
  - ESLint, Stylelint, Prettier для контроля качества кода и стилей.

## Структура проекта

```text
blog-customizer/
├── public/                # HTML-шаблон
├── src/
│   ├── App.tsx            # Корневой компонент приложения
│   ├── ui/                # Переиспользуемые UI-компоненты (формы, инпуты, кнопки, сайдбар)
│   ├── components/        # Логика работы панели настроек и статьи
│   ├── styles/            # Глобальные стили
│   ├── constants/         # Переменные (CSS custom properties)
│   ├── fonts/             # Подключаемые шрифты
│   ├── images/            # Используемые svg
│   └── index.tsx          # Точка входа (root.render)
├── .storybook/            # Конфигурация Storybook
├── webpack/               # Конфиги сборщика
├── package.json           # Скрипты и зависимости
├── tsconfig.json          # Настройки TypeScript
└── README.md              # Описание проекта
```

## Установка и запуск 

Клонируйте репозиторий и установите зависимости:

```bash
   git clone https://github.com/morfekta/blog-customizer.git
   cd blog-customizer
   npm install
```

Для запуска Storybook выполните:

```
npm run storybook
```

Для запуска линтера для стилей выполните:

```
npm run stylelint
```

Для запуска линтера выполните:

```
npm run lint
```

Для запуска форматтера выполните:

```
npm run format
```
